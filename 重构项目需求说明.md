# 牛顿环测量工具 · 重构项目需求说明

> 本文档为需求与决策的唯一梳理入口：§0 为迁移说明，§1~§8 为需求与约定，§9 为实现状态总览，§10 为待补充区。
> 状态标记：✅ 方案已在旧仓库定稿并验证，新仓库按本文档重建　🔶 方案部分验证　⬜ 待实现

## 0. 项目迁移与重建说明

- **新仓库：`physics-newton-ring`**，本文档随迁并继续作为需求与决策唯一入口；新项目的开发请先通读本文档再动手。
- 旧仓库 `newton-ring`（分支 `vue3`）完成了 §9 中标 ✅ 模块的方案验证；后因误操作签出分支导致工作区混乱而**弃用，代码不做整体迁移**，仅本文档与 `public/` 静态资源（opencv.js、favicon.svg）带入新仓库。
- 重建顺序建议：工程骨架 → 主题与布局（含 Logo / LoadingScreen 约定）→ 路由与首页四象限 → 历史存储层 → OpenCV 加载链路 → 识别业务 → 历史 / 数据 / 导出页面。

## 1. 项目定位与部署

- 纯静态单页应用，**无后端**；托管于 GitHub Pages。
- 路由采用 hash 模式（`createWebHashHistory`），兼容静态托管下任意路径直接访问。
- 所有持久化数据（图片、识别结果）仅存于浏览器 localStorage，图片以 dataURL 形式暂存。
- 大型静态资源（opencv.js）放 `public/` 目录原样下发，不进入打包链路。
- localStorage key 前缀统一为 `physics-newton-ring:`（新项目无旧数据迁移负担，key 名跟随新仓库名）。

## 2. 技术栈与工程约束

- 框架/工具：Vue 3、Vite、Naive UI、UnoCSS、VueUse、lodash-es、OpenCV（opencv.js）。
- 依赖管理：**统一使用 pnpm**。
- 代码规范：ESLint + Prettier，采用 Vue 官方推荐配置。
- 不引入 Pinia：全局共享状态用 VueUse `useStorage` 提供响应式数据源（见 §7）。
- 功能实现优先选用**已安装**的 naive-ui / VueUse 组件与工具（如二维码用 naive-ui 内置 `NQrCode`）；确需新增依赖时，必须先与用户确认后再安装。
- 目录约定：`views/` 页面、`components/` 组件、`composables/` 组合式函数、`layouts/` 布局、`router/` 路由、`theme/` 主题、`utils/` 工具、`public/` 静态资源。
- UnoCSS 使用约定（踩坑记录）：
  - 只启用 `presetUno` + `presetIcons`（及 directives / variant-group 两个 transformer），**禁止启用 `presetAttributify`**：本项目无 attributify 写法，而该 preset 会把模板中任意「属性名 + 属性值」解析为 utility 候选，`icon="i-carbon:image-search"` 这类组件 prop 会被误解析为不存在的图标 `carbon:icon-image-search`，产生 Iconify 加载失败告警，并显著扩大 dev 扫描候选量拖慢 CSS 生成。
  - 图标 class（`i-carbon:xxx`，carbon 图标集）必须以**字符串字面量**出现在源码中（UnoCSS 静态提取，不识别运行时拼接的 class 名）；菜单 / 首页入口卡片所用图标均以字面量写在 `src/router/index.js` 的 `meta.icon`，并由 `uno.config.js` 的 pipeline 把 `src/**/*.js` 纳入扫描。
  - 修改 `uno.config.js` 或图标用法后必须**重启 dev server 并硬刷新**验证，避免缓存残留造成误判（旧仓库曾出现「首页菜单图标不渲染」，经 `createGenerator` 验证 `i-carbon:home` 的 CSS 可正常生成，疑似 dev 缓存 / HMR 残留）。
- **naive-ui 样式注入顺序约定**：`index.html` 的 `<head>` **末尾**放 `<meta name="naive-ui-style" />` + `<meta name="vueuc-style" />` 两个锚点（顺序不可颠倒，且必须是 head 的最后几个元素）。naive-ui / vueuc 运行时注入的 `<style>` 会被插到各自 meta **之前**，我们的 UnoCSS / `global.css`（dev 为运行时追加的 `<style>`、build 为 Vite 追加到 head 末尾的 `<link>`）恒定排在**之后** → 同特异性时以原子类为准，不需 `!important`。依据：官网「潜在的样式冲突」+ `css-render/mount.js` 的 `insertBefore(target, anchorMetaEl)` 实现。注：`inline-theme-disabled` 走 `useThemeClass` 注入的 `--n-*` 变量类不带锚点（append 到 head 末尾），但它只定义自定义属性，不与原子类抢同一 CSS 属性。
- **naive-ui 组件配色优先走 `themeOverrides`（含 `peers.*` 组件级覆盖），而不是用原子类压组件背景**：如侧栏底色用 `peers.Layout.siderColor`、页头直接用 `n-layout-header bordered`（底色与分割线由 `headerColor` / `headerBorderColor` 给出）。原子类覆盖仅在 naive-ui 无对应主题键时使用。

## 3. 视觉与主题

- 整体**深色调**，主色**蓝色**（`#2080f0`）。
- 选色需考虑讲课时投影曝光：各页面背景色基本保持一致（基调 `#101014`，卡片 `#18181c`，布局 `#0a0a0d`），保证投影对比度与可读性。
- 水印：复用 Naive UI `NWatermark` 全屏水印；顶栏提供开关，讲课时可临时关闭以减少曝光干扰。✅
- 黑白（深/浅）主题切换：复用 Naive UI 主题能力。✅（`useTheme` 组合式函数 + VueUse `useStorage` 持久化 `physics-newton-ring:theme`；头部右上角圆形按钮切换；单一色板 `palette` 派生 CSS 变量与 Naive UI `themeOverrides`）

## 4. 布局与导航

> 2026-09-13 布局重构（参考 naive-ui 官网）：页头全宽固定不滚动，其下左右分栏；
> `BasicLayout` 拆为 `AppHeader` / `AppSider` / `AppNavMenu` / `AppContent` / `AppFooter` 五个布局组件（均在 `src/layouts/`，`src/components/SideNav.vue` 已删除）。
> 布局壳：最外层为原生 `div.flex.h-screen.flex-col.overflow-hidden`（naive-ui 无「纵向堆叠 + 头部固定」原语，只有绝对定位一条路），其下整块分栏区用 `n-layout :has-sider` 承载；页脚在 `AppContent` 的滚动容器内做 sticky footer。

- 页头（`AppHeader`）：`h-14` **固定置顶、不随内容滚动、横跨整个视口宽度**（侧栏在其下方）；左侧菜单按钮 + Logo + 站名「牛顿环测量工具」，右侧 §3 水印开关 + 深/浅主题切换。✅（用 `n-layout-header bordered`，底色 / 分割线走 naive-ui 主题键 `headerColor`（= `cardColor`）与 `headerBorderColor`（= `dividerColor`），不写 `bg-card` / border 类；`.n-layout-header` 无 `flex-shrink: 0`，需自己上 `shrink-0`）
- 主内容（`AppContent`）：滚动只发生在内容区内部（naive-ui `n-scrollbar`，由 `n-layout-content :native-scrollbar="false"` 承载）；顶部渲染路由 `meta.title` 大标题 + `meta.description` 副标题；内容**靠左对齐**（各视图根容器去掉 `mx-auto`、保留 `max-w-*`）。✅（底色不写 `bg-base`：`.n-layout-content` 的 `background-color` = `Layout.color` = `common.bodyColor` = `palette.base`，与 `--c-base` 同源）
- 页脚（`AppFooter`）：**sticky footer 形态** —— 在 `AppContent` 的滚动容器内、但不常驻占用底部空间：内容不足一屏时贴底，内容长时随内容滚动。横向仍与左侧边栏紧挨，底色用 `bg-layout`（= `--c-layout` = 侧栏的 `peers.Layout.siderColor`），与侧栏同色面。✅
  - **实现要点**：`min-height: 100%; display: flex; flex-direction: column` 必须通过 `n-layout-content` 的 **`content-style`** 下推到 `.n-scrollbar-content`（其父级 `.n-scrollbar-container` 是 `height: 100%` 的确定高度），内容块用 `flex-grow`（非 `flex-1`，保持 `flex-basis: auto`，自动高度容器的 intrinsic 尺寸计算更稳）撑开、页脚 `shrink-0`。另：`opacity` 只能给页脚内的文字容器，给 `footer` 本身会连 `bg-layout` 一起半透，与背层 `bodyColor` 混色后就和侧栏底色对不上。
  - **两个踩过的方式为何不行**：① 给 `<app-content>`（`.n-layout-content` 根）加 `min-h-full` 并与页脚做 flex-col 兄弟 → 内容区至少等于整列高，加上页脚必超出，外层 `.n-layout{overflow:hidden}` 直接裁掉页脚且无滚动条可达；② 把 `min-h-full` 写在滚动容器**内层** div → 百分比 `min-height` 需参照父级确定高度，而 `.n-scrollbar-content` 高度为 auto，百分比按 auto 处理，等于没写。
- PC / 平板（≥768px）：左侧常驻可折叠侧栏（`AppSider`，240px / 折叠 64px，内置宽度过渡），**高度铺满页头以下区域、菜单内部独立滚动**。✅（侧栏底色走 `themeOverrides.peers.Layout.siderColor = palette.layout`，不再用 `bg-layout` 原子类覆盖；根元素保留 `relative` 作为二维码吸底的定位锚）
- 移动端（<768px）：页头汉堡按钮 + **左抽屉**菜单（抽屉内 `n-scrollbar` 独立滚动），点击菜单后自动关闭；抽屉不渲染二维码（手机用户已在手机上，扫码无意义）。✅
- 首页为**功能入口卡片网格**：直接循环渲染路由表（排除首页自身），标题 / 图标 / 说明均取路由 `meta`。✅（原 `meta.quadrant` 四象限筛选已废弃删除，`meta.description` 为新增字段）
- 菜单项由路由表单一来源生成（`useMenu`），避免菜单与路由重复维护。✅
- 站点 Logo 用 `public/favicon.svg`（深色圆角底 + 蓝色同心圆牛顿环标记）以 `<img>` 渲染，**不再使用图标字体** `i-carbon:circle-dash`；favicon.svg 同时作为站点 favicon 与**页头** Logo 的唯一来源。✅（`AppHeader` 用 `import.meta.env.BASE_URL + favicon.svg` 渲染；原侧栏顶部 Logo 行已上移到页头）
- 左侧菜单**底部放置站点二维码**（折叠时隐藏），方便讲课时手机扫码打开。✅（`SiteQrcode.vue` 用 naive-ui 内置 `NQrCode`（自带 qrcodegen、零外部依赖）按 `location.origin + location.pathname` 渲染，absolute 锚定侧栏底部，任意 host 自适应）
- 响应式断点基于 VueUse `breakpointsTailwind`：mobile < 768px ≤ tablet < 1024px ≤ desktop。✅
- 路由切换 `fade-slide` 过渡动画（挂在 `AppContent` 的 `router-view` 上）；面包屑因单层扁平导航省略。✅

## 5. OpenCV 引擎与加载策略（决策记录）

- 原始需求：opencv 库较大，首屏给一个简单转圈 loading。
- **当前决策（第四版：业务按需触发）**：
  1. 引擎文件固化在 `public/opencv.js`（~10MB UMD，版本 4.10.0-release.1），不经过打包器，避免 dev 预打包转换与浏览器解析巨型 chunk 造成卡顿；
  2. **路由层不预热**：`/recognition` 为普通懒加载路由；`loadOpenCv()` 由**真正使用 cv 的业务代码**（上传 / 识别按钮 handler）按需触发，配合按钮 loading 态与 `LoadingScreen` 遮罩。原因：路由层预热（defineAsyncComponent loader 并行拉引擎）会在进入页面时让主线程 parse/eval 10MB UMD 而冻结数秒，且占位态的识别页根本不用 cv，预热代价白白付出；用户主动点击时触发则有明确交互预期，卡顿感知低；
  3. 加载方式：注入 `async` 经典 script，以 50ms 轮询 `window.cv` 检测下载执行完成，再等 `onRuntimeInitialized` 确认 wasm 就绪；script error 时 reject 并移除节点，保证重试幂等；
  4. `LoadingScreen` 样式收敛：全屏深色遮罩**只保留缺口转圈（NSpin）+ 加载文字**（如「正在加载图像处理引擎…」），失败态显示重试入口；**去掉顶部 logo 与介绍文字**。
- 决策演进：首屏全局预热 → 识别页异步组件并行加载 → public 静态脚本 + 路由层预热 → **public 静态脚本 + 业务按需触发**（当前；每次演进均为解决卡顿 / 加载失败问题）。
- 遗留成本：wasm 实例化瞬间主线程仍有短暂冻结（经典 script 执行的固有特性）；如未来需要零冻结，评估迁移 Web Worker。

## 6. 功能模块需求

### 6.1 识别（核心页面）

- 上传 / 拍摄牛顿环图片，图片 localStorage 本地暂存（复用 Naive UI 上传组件）。✅（点击/拖拽上传 + 存入前压缩）
- 基于 OpenCV 识别同心环，自动定位圆心与各级环半径。✅（两步流程：预处理→自动检测圆心→人工拖拽微调→径向剖面识别暗环）
- 画布编辑交互：点击**空区域**弹提示「加一环」；点击**环上**弹「修改 / 删除」。🔶（实现为 **Zoom 放大模态补环**（滚轮缩放/平移/点击手动补环+合并重编号）+ 环列表启用/取消勾选/悬停高亮联动，未按原「点击空区域/环上弹提示」交互 1:1 复刻）
- 识别结果实时预览，并同步到历史记录。✅（计算链 computed 实时预览；保存写 session + 入历史）
- 操作流程用 Naive UI **步骤组件**（NSteps）组织（如：导入 → 识别 → 编辑 → 完成）。🔶（改为**自定义两步流程 UI**，未使用 NSteps 组件）
- 上传 / 识别动作触发前 `await loadOpenCv()`（见 §5），加载期间显示 `LoadingScreen`，失败提供幂等重试；页面组件本身不预载引擎。🔶（`loadOpenCv()` 已按需接入；`LoadingScreen.vue` 组件已建但**未接线**，当前用按钮 loading 态代替遮罩）

### 6.2 历史记录

- 左侧菜单入口；因内容为图片，用 **2×2 方格**罗列。✅（2×2 方格 + 查看/删除/清空/恢复会话）
- 支持查看、删除、清空等管理操作（存储层已提供增改删清能力）。✅

### 6.3 数据展示

- 左侧菜单入口；支持将以前**导出的数据表再导入**回来展示。✅（JSON 导入回显，与导出互逆；复用 ResultTables 展示表1/表2/不确定度五步）

### 6.4 导出

- 下载识别后的图片。✅
- 导出识别数据的 JSON。✅
- 导出识别数据的表格视图 / 网页视图。✅（另含 CSV 1:1 复刻，含 BOM + 不确定度行）

## 7. 数据结构约定

历史记录单条结构（`useHistoryStore`，localStorage key：`newton-ring:history`）：

```js
{
  id: string,          // 唯一 id（crypto.randomUUID）
  title: string,       // 名称
  image: string,       // dataURL 形式的图片（localStorage 暂存）
  createdAt: number,   // 时间戳
  data: object | null, // 识别 / 测量数据（环序号、半径等）
}
```

- 识别 / 历史 / 数据展示三个页面共享同一份响应式数据源。
- 导出 JSON 的字段结构待识别功能落地时定稿（需与「数据展示」的导入格式互为逆操作）。

## 8. 页面清单

| 路由 | 页面 | 说明 |
|---|---|---|
| `/` | 首页 | 功能入口卡片网格（循环渲染路由表，见 §4） |
| `/recognition` | 识别 | 核心业务页，OpenCV 由业务按需触发加载（见 §5） |
| `/calibration` | 像素标定 | 独立模块（旧 Tab1）：双图对齐/圆形截取/PSR 闸门/取点/标定值，见 §11.5-1 |
| `/history` | 历史记录 | 2×2 图片方格 |
| `/data` | 数据展示 | 导入已导出的数据表展示 |
| `/export` | 导出 | 图片 / JSON / 表格视图 / 网页视图 |

## 9. 实现状态总览

> 状态以「旧仓库 newton-ring/vue3 的方案验证」为准：✅ 表示方案已在旧仓库定稿并验证通过，新仓库 `physics-newton-ring` 按本文档约定直接重建即可；新仓库自身从零开始。

| 模块 | 状态 | 备注 |
|---|---|---|
| 工程骨架（Vite/ESLint/Prettier/pnpm/UnoCSS） | ✅ | |
| 路由 + hash 模式 + 标题联动 | ✅ | |
| 布局（页头全宽固定 / PC 可折叠侧栏 / sticky 页脚 / 移动抽屉 / 路由过渡） | ✅ | 拆为 `AppHeader`/`AppSider`/`AppNavMenu`/`AppContent`/`AppFooter`，见 §4；面包屑因单层扁平导航省略 |
| 首页功能入口卡片 | ✅ | 原「四象限」，现直接循环渲染路由表 |
| 深色主题 + 蓝色主色 | ✅ | 深/浅切换已落实，见 §3 |
| 水印 + 开关 | ✅ | |
| 响应式断点 | ✅ | |
| 历史存储层（localStorage） | ✅ | 页面 UI 已完成 |
| OpenCV 按需加载链路 | ✅ | 见 §5 决策记录（新仓库按「业务按需触发」版落实） |
| UnoCSS 图标链路（禁 attributify + 字面量提取） | ✅ | 见 §2 约定，attributify 踩坑已记录 |
| Logo 改用 favicon.svg / LoadingScreen 样式收敛 | 🔶 | Logo 已落实；LoadingScreen.vue 已建但未接线（见 §6.1） |
| 菜单底部二维码 | ✅ | 见 §4；运行时生成当前 host |
| 深/浅主题切换 | ✅ | 见 §3；useStorage 持久化 |
| 算法层抽芯（imageProcessor/ringFitter/imageRegistrator/canvasDrawer） | ✅ | 旧 `js/` 纯算法 1:1 搬入 `utils/`，剥离 DOM/旧框架耦合（§11.5-2） |
| 共享层（resultIO/useMeasureStore/ResultTables/interactionHandler） | ✅ | 序列化单一来源 + 跨页单例 + 表格共用组件 + DOM 交互抽芯 |
| 识别业务（上传/两步识别/Zoom 补环/环列表/计算链） | ✅ | 核心已开发；NSteps/LoadingScreen/画布点击交互有差异，见 §6.1 与 §12 |
| 像素标定页 `/calibration`（双图对齐/圆形截取/取点/标定值） | ✅ | 新增独立路由，§11.5-1；辅助项部分延迟，见 §12 |
| 历史记录页 UI | ✅ | 2×2 方格 + 查看/删除/清空/恢复会话 |
| 数据展示页（导入展示） | ✅ | JSON 导入回显，与导出互逆 |
| 导出（图片/JSON/表格/网页视图/CSV） | ✅ | 全保留（§11.5-3） |
| access-lock（软锁，含 2026/9/25 自动失效） | ✅ | App.vue 接入（§11.5-7） |

## 10. 待补充

- 识别算法细节：环级数范围、半径精度要求、圆心允许偏差等待定稿。
- 导出表格视图 / 网页视图的具体列与样式待定稿。
- 二维码内容 = 当前 host（`location.origin + location.pathname`）；生成方式 = naive-ui 内置 `NQrCode` 组件渲染（自带 qrcodegen、零外部依赖，已定，见 §4）。
- 其他业务需求后续继续补充于此。

### 已解决并定稿（无需再议）

- `presetAttributify` 踩坑与禁用约定 → §2。
- OpenCV 路由层预热导致进页冻结，改为业务按需触发 → §5。
- `LoadingScreen` 只保留转圈 + 文字 → §5。
- 侧栏 Logo 改用 `favicon.svg` → §4（已再次调整：Logo 与站名上移至页头，见 §4）。
- 旧仓库「首页菜单图标不渲染」：经 `createGenerator` 验证当前配置下 `i-carbon:home` 的 CSS 可正常生成，疑似 dev 缓存 / HMR 残留；新项目以「图标类名写字面量 + `src/**/*.js` 纳入扫描 + 修改后重启硬刷新」约定（§2）规避，若复现优先排查缓存而非图标集。

## 11. MVP-0 工程骨架决策（2026-09-13 定稿）

> 本节把 MVP-0（工程骨架）阶段的决策沉淀入文档，是对 §2 / §4 / §9 的落地补充。
> MVP-0 目标：Vue3 + Vite 配置齐全、`pnpm dev` 能启动「带最简布局壳的五个占位页」。

### 11.1 决策清单

| 决策点 | 结论 | 理由 / 关联 |
|---|---|---|
| 语言 | **JavaScript**（不用 TS） | 维护者前端基础弱；旧 `js/` 算法移植零摩擦；用 JSDoc + ESLint 补回安全性 |
| 包管理器 | **pnpm** | 遵循 §2；Node 22 自带 corepack（`corepack enable pnpm`） |
| 地基范围 | **一次配齐** Vue3 + Vite + vue-router(hash) + Naive UI + UnoCSS + VueUse + lodash-es + ESLint + Prettier | §9 将「工程骨架」作整体验收；库间配置耦合，后补更痛。`opencv.js` 不属地基（§5，留迁移阶段） |
| 页面 | 按 §8 建 **5 个真实路由页**，内容仅「页面名 + 开发中…」占位 | 冒烟验证 router / Naive UI / UnoCSS / 主题 四条链路 |
| 布局壳 | **最简静态壳**：左侧固定菜单 + 顶栏 + `router-view` | 折叠 / 移动抽屉 / 面包屑留「主题与布局」里程碑 |
| 菜单数据源 | **路由表单一来源**：`useMenu` 读 `routes` 生成 `n-menu` options（key=path） | §4；升级到折叠 / 抽屉时数据源不返工 |
| Naive UI 引入 | **显式 import**（不用 unplugin 自动导入） | 新手透明、无魔法、依赖更少 |
| Vite `base` | **`'./'`**（相对，dev / build 一致） | 见 §11.2 |
| 路由 base | **`createWebHashHistory()` 不传参**，运行时按 `location` 推导 | 最大化可移植 |
| favicon | `public/favicon.svg` 先给**草稿版**（深色圆角底 + 蓝色同心环） | §4；正式设计待定 |
| ESLint | flat config + `eslint-plugin-vue` recommended + `eslint-config-prettier` | §2「Vue 官方推荐配置」 |

### 11.2 `base: './'` + hash 路由 = 可移植部署（重要耦合）

- **决策**：`vite.config.js` 用相对 `base: './'`，配合 hash 路由，使产物可原样放在**任意服务器 / GitHub Pages 项目子路径**下，无需按部署路径改配置。
- **成立原理**：hash 模式下文档真实路径恒为 `index.html`（只有 `#/xxx` 变化），相对资源（js / css / 图片 / 懒加载 chunk）始终相对同一位置解析。
- **⚠️ 强约束**：相对 base **只在 hash 路由下安全**；若改用 `createWebHistory`（HTML5 history），嵌套路由会使 `./` 资源解析错乱。§1 已定 hash 模式，务必保持。

### 11.3 MVP-0 文件清单

- 工程配置：`package.json`、`vite.config.js`、`uno.config.js`、`eslint.config.js`、`.prettierrc.json`、`.prettierignore`、`.gitignore`、`jsconfig.json`、`index.html`
- 源码：`src/main.js`、`src/App.vue`、`src/router/index.js`、`src/layouts/BasicLayout.vue`、`src/composables/useMenu.js`、`src/theme/index.js`、`src/styles/global.css`、`src/components/PagePlaceholder.vue`、`src/views/{Home,Recognition,History,Data,Export}View.vue`
- 静态：`public/favicon.svg`

### 11.4 验收标准（MVP-0 完成定义）

- `pnpm dev` 打开 `http://localhost:5173/` 无报错；
- 深色界面：左侧 240px 菜单（logo + 5 项）、顶栏、内容区「首页 · 开发中…」卡片；
- 点菜单 → `#/xxx` 跳转 + 内容切换 + 标题联动；`i-carbon:*` 图标正常（safelist 生效）。

### 11.5 业务迁移决策（2026-09-13 拷问定稿）

1. **像素标定**：完整迁移旧 Tab1（双图对齐/圆形截取/PSR 闸门/取点），作为独立模块纳入。
2. **算法迁移方式**：抽芯移植——旧 `js/` 已验证纯算法 1:1 搬入 `utils/`，剥离 DOM/旧框架耦合，公式与阈值不变。
3. **导出格式**：全保留（图片/JSON/表格视图/网页视图/CSV）；JSON 为唯一与「数据展示」导入互逆的格式。
4. **不确定度 + 修约**：1:1 保留（A+B 五步评定 p=0.683,k=1,Δ=0.002；roundHalfEven 等四函数）。
5. **逐差法**：保留（步长 m−n 默认 5、动态分组、无组数上限）。
6. **存储**：前缀统一 `physics-newton-ring:`（§7 旧 key 作废）；图片爆仓 = 存入前压缩（限最长边+JPEG 质量）+ 条数上限 + LRU 淘汰。
7. **access-lock**：原样搬入（含 2026/9/25 自动失效逻辑）。

## 12. 迁移进度快照（2026-09-13 更新，供换电脑继续开发）

> 本节为接手快照：旧仓 `newton-ring` 全部主功能已迁入新仓 `physics-newton-ring`。

### 12.1 当前状态

- **已迁移 6 个页面**：`/home` `/recognition` `/calibration` `/history` `/data` `/export`；共享层（`resultIO` `useMeasureStore` `ResultTables` `interactionHandler`）+ 算法层抽芯全部就位。
- **质量门**：`npx eslint src` 0 错误；`pnpm build` 成功（最大 chunk index ~521kB）。
- **⚠️ 未 commit**：上述所有改动仅在工作区（`git status` 显示大量 A/M），**尚未提交**。换电脑前务必先 `git add -A && git commit && git push`，否则新机器拉不到。

### 12.2 环境启动

- Node 22.14 + `corepack enable pnpm`；安装依赖 `pnpm install`；开发 `pnpm dev`；构建 `pnpm build`。
- OpenCV 固化在 `public/opencv.js`（~10MB，不进打包），由业务按需 `loadOpenCv()` 触发。

### 12.3 延迟/差异项（待补，非阻塞）

1. **标定页辅助对齐圆环（guide rings）** — 叠加画面参考同心圆，纯视觉辅助（旧 #25）。未做。
2. **实时重合度残差防抖显示** — 已改为按需「检查对齐」返回 PSR，交互等价但非实时（旧 #26 部分）。
3. **对齐自检控制台钩子 `window.__alignSelfCheck`** — 调试用，非 UI。未做（建议可略）。
4. **识别页「导入标定图 A/B」跨页按钮**（`importCalibAToRings/B`）— 从识别页直接拉标定图待识别（旧 #10）。未做。
5. **NSteps 步骤条** — §6.1 原需求，已改为自定义两步流程 UI（待定是否补回）。
6. **LoadingScreen 遮罩接线** — 组件已建但未引用，当前用按钮 loading 态代替（待定是否接线）。

### 12.4 建议下一步

- 先 commit/push 当前成果 → 新机器 `pnpm install` → `pnpm dev` 做浏览器运行时冒烟验收（识别/标定/导出全链路）→ 再决定是否补齐 12.3 中的 #1/#4 真实用户可见项。
